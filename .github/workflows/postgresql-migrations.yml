name: Actions Platform Probe

on:
  push:
    branches:
      - "ci/**"

permissions:
  contents: read

jobs:
  probe:
    name: Actions Platform Probe
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Confirm runner startup
        run: echo "actions-platform-probe-ok"
